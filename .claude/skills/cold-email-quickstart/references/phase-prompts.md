# Phase Prompts — MISSING

The original `references/phase-prompts.md` (the user-facing copy referenced as
`§P0.resume`, `§P1.instantly`, `§P6.optimize_gate`, etc. in `SKILL.md`) was not
included when this skill was installed manually.

Until the real file is added, anyone running the wizard should improvise the
phase-gate copy from the phase descriptions in `SKILL.md` instead of quoting
`§` references.

To fix: obtain `phase-prompts.md` from the `cold-email-quickstart` skill
distribution and replace this file.
